# Contributing to Auto-CUMCM

感谢你为 Auto-CUMCM 提交改进。这个仓库的目标是让数学建模比赛中的 Agent 工作流更可靠、更容易复核。

## 适合贡献的内容

- 新的 CUMCM 题型处理流程或参考资料；
- 图表、表格、论文和提交打包的质量检查；
- 能独立运行、错误信息清晰的辅助脚本；
- 不含真实敏感信息的最小示例和回归用例；
- README、技能边界和使用说明的改进。

## 技能约定

每个技能必须包含一个位于目录根部的 `SKILL.md`，并说明：

1. 技能名称和明确的触发条件；
2. 适用场景与不适用场景；
3. 输入、执行步骤和预期产物；
4. 需要使用的脚本或参考资料；
5. 可执行的验证方式。

优先扩展现有技能，只有在职责清晰且无法归入现有技能时才新增目录。

## 提交前检查

```bash
git diff --check
find . -type f \( -name '__pycache__' -o -name '*.pyc' \)
```

不要提交个人信息、学校/队伍身份信息、竞赛保密材料、编辑器缓存或临时生成物。若修改了脚本，请同时更新对应技能的使用说明和最小示例。

## Pull Request 建议

标题应直接说明变更，例如 `Improve release scanner for xlsx metadata`。描述中请写清：

- 解决了什么问题；
- 修改影响哪些技能；
- 如何验证；
- 是否新增或改变了用户可见的行为。

小而聚焦的 Pull Request 更容易审阅和合并。
